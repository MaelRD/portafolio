// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  output: 'static', // Asegúrate de que esto esté configurado // Asegúrate de que esto esté configurado
  base: '/portfolio', // Nombre del repositorio en GitHub
});
